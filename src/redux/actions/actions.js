// combine all action into single file - better import

import toggleMenu from "./menu";
import createUser from "./tableActions";

const actions = { toggleMenu, createUser };

export default actions;
